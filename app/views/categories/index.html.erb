<div class="row">
  <div class="col-md-2 col-md-offset-1">
    <ul class="list-item">
      <% @categories.each do |category| %>
        <li class="item">
          <%= link_to category.name, category_path(category), remote: true %>
        </li>
      <% end %>
    </ul>
  </div>
  <div class="col-md-8 list-book"></div>
</div>
